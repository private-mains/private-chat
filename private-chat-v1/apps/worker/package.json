{
  "name": "private-chat-v1-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute private-chat-db --file=./migrations/0001_init.sql --remote"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.6.0"
  }
}
